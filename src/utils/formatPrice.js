// Step 1: Create a function that takes a price in cents

  // Step 2: Convert the cents into dollars

  // Step 3: Use toLocaleString to format it into USD currency

// Step 4: Export the function so it can be reused
